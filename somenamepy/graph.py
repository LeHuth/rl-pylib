import numpy as np
class graph:

    def __init__(self, points, uid, func, x, slope, b, lines):
        self._points = points
        self._uid = uid
        self._func = func
        self._x = x
        self._slope = slope
        self._b = b
        self._lines = lines


    def appendPoint(self,point):
        pass
