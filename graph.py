import numpy as np
class graph:

    def __init__(self, points, id, func):
        self._points = np.array(points)
        self._id = id
        self._func = func

    def appendPoint(self,point):
        pass
